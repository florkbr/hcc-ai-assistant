# MCP Discovery Server Configuration
# This file lists the MCP servers to discover and index
# Separate from lightspeed-stack.yaml to maintain separation of concerns

mcp_servers:
  # - name: "local-mcp-server"
  #   provider_id: "local-mcp-provider"
  #   url: "http://host.containers.internal:3001/mcp"
  #   authorization_headers:
  #     x-rh-identity: "kubernetes"
